# 解决 ModuleNotFoundError 错误

## 🔍 问题原因

你看到 `ModuleNotFoundError: No module named 'sqlalchemy'` 是因为：
- 你的目录中有 `venv` 虚拟环境目录
- 但是**没有激活虚拟环境**
- Python 使用的是系统 Python，而不是虚拟环境中的 Python
- 系统 Python 中没有安装项目依赖

## ✅ 解决方法

### 方法一：使用我创建的初始化脚本（最简单）

我已经为你创建了一个自动脚本，它会：
1. 自动激活虚拟环境
2. 自动安装依赖
3. 运行数据初始化

**运行方式**：
```bash
cd "/Users/sixsevensconsubine/Desktop/阅心/backend"
./init_data.sh
```

### 方法二：手动激活虚拟环境（推荐学习）

**步骤 1：激活虚拟环境**

```bash
cd "/Users/sixsevensconsubine/Desktop/阅心/backend"
source venv/bin/activate
```

**激活成功后，你的终端提示符会变成**：
```
(venv) sixsevensconsubine@MacBook backend %
```
注意前面有 `(venv)` 标识！

**步骤 2：确认虚拟环境已激活**

运行：
```bash
which python
```

**应该显示**：
```
/Users/sixsevensconsubine/Desktop/阅心/backend/venv/bin/python
```

如果显示的是系统 Python（比如 `/usr/bin/python`），说明虚拟环境没有激活成功。

**步骤 3：安装依赖**

```bash
pip install -r requirements.txt
```

这会安装所有需要的包，包括 sqlalchemy。

**步骤 4：运行初始化脚本**

```bash
python scripts/init_books.py
```

## 📝 完整操作流程

```bash
# 1. 进入 backend 目录
cd "/Users/sixsevensconsubine/Desktop/阅心/backend"

# 2. 激活虚拟环境（重要！）
source venv/bin/activate

# 3. 确认激活成功（应该看到 (venv) 前缀）
# 终端提示符应该变成：(venv) ...

# 4. 安装依赖（如果还没安装）
pip install -r requirements.txt

# 5. 运行初始化脚本
python scripts/init_books.py
```

## ⚠️ 常见错误

### 错误 1：忘记激活虚拟环境

**症状**：运行脚本后提示 `ModuleNotFoundError`

**解决**：先运行 `source venv/bin/activate`

### 错误 2：虚拟环境中没有安装依赖

**症状**：激活了虚拟环境，但还是提示 `ModuleNotFoundError`

**解决**：运行 `pip install -r requirements.txt`

### 错误 3：在错误的目录运行

**症状**：提示找不到 `requirements.txt`

**解决**：确保在 `backend` 目录下运行

## 🔍 如何确认虚拟环境已激活？

### 方法 1：查看终端提示符

激活后，提示符前面应该有 `(venv)`：
```
(venv) sixsevensconsubine@MacBook backend %
```

### 方法 2：运行 which python

```bash
which python
```

**激活后应该显示**：
```
/Users/sixsevensconsubine/Desktop/阅心/backend/venv/bin/python
```

**未激活会显示**：
```
/usr/bin/python
# 或其他系统 Python 路径
```

### 方法 3：运行 pip list

```bash
pip list
```

如果看到 `sqlalchemy`、`fastapi` 等包，说明虚拟环境已激活且依赖已安装。

## 💡 为什么需要虚拟环境？

虚拟环境可以：
- 隔离项目依赖，不影响系统 Python
- 确保每个项目使用正确的包版本
- 避免包冲突

## 🚀 快速解决方案

**最简单的方式**，直接运行：

```bash
cd "/Users/sixsevensconsubine/Desktop/阅心/backend"
./init_data.sh
```

这个脚本会自动处理所有步骤！
